## Packages
ethers | Required for browser-based smart contract deployment using ethers v6
date-fns | Required for formatting deployment timestamps

## Notes
- Deployment happens entirely in the browser.
- Private keys must never be logged or sent to the backend.
- UI features a clean, minimal dashboard split into "Deploy Form" and "Deployment History".
